# SQIsign-Qlapoti Level III portable companion

The portable pqm4 implementation is in `ref/`. Install the overlay, then
inject the same content-addressed Level-III export used by the target.
It exists for host testvectors and algorithm/wire-format comparison; the target
implementation is `crypto_sign/sqisign-qlapoti-lvl3/m4/`.
