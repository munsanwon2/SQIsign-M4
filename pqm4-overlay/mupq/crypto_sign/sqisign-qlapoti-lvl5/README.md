# SQIsign-Qlapoti Level V portable companion

The portable pqm4 implementation is in `ref/`. Install the overlay, then
inject the same content-addressed Level-V export used by the target.
It exists for host testvectors and algorithm/wire-format comparison; the target
implementation is `crypto_sign/sqisign-qlapoti-lvl5/m4/`.
